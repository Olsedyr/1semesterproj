package worldOfZuul;

//Choice Events like choosing the mean of transport or how to take a bath

public class EventItem extends Item{
    public EventItem(String itemDescirption, int points, boolean toggleState, boolean pickable) {
        super(itemDescirption, points, toggleState, pickable);
    }
}
